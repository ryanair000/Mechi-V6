# Supabase — use the modern publishable key, never a service role key in NEXT_PUBLIC_*
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Public canonical origin. Change when the final domain is connected.
NEXT_PUBLIC_APP_URL=http://localhost:3000
